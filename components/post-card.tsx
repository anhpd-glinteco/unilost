import Link from "next/link"; import type { Post } from "@/lib/types";
export function PostCard({ post }: { post: Post }) { return <Link href={`/posts/${post.id}`} className="group block rounded-[1.25rem] border border-[#dfe9e2] bg-white p-4 transition hover:-translate-y-1 hover:border-[#a6c7ba] hover:shadow-lg"><div className="flex items-start justify-between"><div className="grid h-24 w-24 place-items-center rounded-xl bg-[#f3dfc7] text-4xl">{post.emoji}</div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${post.type === "lost" ? "bg-[#fff0e4] text-[#b65e2b]" : "bg-[#e7f2ec] text-[#28725c]"}`}>{post.type === "lost" ? "ĐANG TÌM" : "ĐÃ NHẶT ĐƯỢC"}</span></div><h3 className="mt-4 line-clamp-1 font-serif text-xl group-hover:text-[#d87642]">{post.title}</h3><p className="mt-2 text-sm text-[#6f817a]">{post.location} · {post.color}</p><div className="mt-4 flex items-center justify-between border-t border-[#edf2ee] pt-3 text-xs text-[#83958d]"><span>{post.category}</span><span>{new Date(post.createdAt).toLocaleDateString("vi-VN")}</span></div></Link>; }
